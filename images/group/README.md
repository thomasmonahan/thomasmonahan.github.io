# Group member photos

Drop member photos into this directory and reference them from
`_data/group.yml` via the `photo:` field (e.g. `photo: jane_doe.jpg`).

Photos are displayed as circular thumbnails. Square source images (around
400x400 px) look best, but the CSS will crop non-square images to fit.
